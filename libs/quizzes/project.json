{
  "name": "quizzes",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/quizzes/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:api"],
  "targets": {
    "lint": {},
    "test": {},
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --build libs/quizzes/tsconfig.lib.json --emitDeclarationOnly"
      }
    }
  }
}
